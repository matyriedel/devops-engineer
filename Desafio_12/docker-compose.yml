version: '3'

services:
  web_server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "82:80"
    depends_on:
      - db

  db:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: example
